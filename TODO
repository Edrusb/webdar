+ faire que les listener objects utilisent un pointeur sur central object pour logger les infos
+ faire des parser bouchon pour pouvoir compiler et tester cette partie (webdar + central_report + listener)
+ revoir les entetes HPP avec #ifndef
+ revoir l'ordre des include
+ ne plus se baser sur libdar::U_I virer autant que possible les reference et ne pas inclure de using namespace libdar
+ implementer connexion::read & write
+ tester l'implementation actuelle de parser (hello word)
- keepalive dans session TCP
- pouvoir saisir des nom et convertir les IP en nom si possible (peer IP)
- revoir sterrror_r dans exception_system (strerror n'est pas thread safe)
- finir la gestion des signals et masks (avec les informations se sessions ouvertes etc.)


- finir l'implementation de la class parser pour la gestion du protocole HTTP (requetes / cookies & Co).
- ajouter la gestion des signaux au sein des threads
- prendre en compte les signaux qui peuvent etre retournes par une socket

- revoir toutes les allocations dynamiques en "new (nothrow)"
- ajouter des #ifdef dans les includes
- ajouter la licence en CLI -L, entetes des fichiers avec GPLv3
- portabilite avec automake / autoconf
- internationalisation avec gettext
